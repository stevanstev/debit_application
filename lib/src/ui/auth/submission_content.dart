import 'package:debit/src/ui/auth/submission/submission_card.dart';
import 'package:flutter/material.dart';

class SubmissionContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        SubmissionCard(),
      ],
    );
  }
}
